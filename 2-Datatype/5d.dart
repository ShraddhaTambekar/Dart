void main(){

     bool flag = true;
     print(flag);       

     flag = 0;        //Error: A value of type 'int' can't be assigned to a variable of type 'bool'.
     print(flag);     //The numeric values 1,0 cannot be used to represent the true or false value.

}